applications:
  - path: .
    name: goseed
    command: goseed server -config config.json
    env:
      GOPACKAGENAME: goseed
      GOVERSION: go1.10
    memory: 512M
    instances: 1
    disk_quota: 1024M
    services: []
